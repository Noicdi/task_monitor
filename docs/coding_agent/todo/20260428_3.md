# 任务监视器开发 TODO

## 1. 当前状态

- [x] 阅读 `docs/design/1.txt`
- [x] 检查当前仓库目录
- [x] 确认当前仓库尚无源码和测试
- [x] 阅读用户在 `research/20260428_1.md` 中追加的 `// FIX`
- [x] 阅读用户在 `research/20260428_2.md` 中追加的 `// BUG`
- [x] 将结构化日志路径修正纳入 `research/20260428_3.md`
- [x] 将结构化日志路径修正纳入 `plan/20260428_3.md`
- [x] 生成本轮 `todo/20260428_3.md`
- [x] 用户已确认编码执行

## 2. 已确认编码规则

- [x] 配置错误且无法加载飞书 Webhook 时，只写日志并退出
- [x] `monitor_log_dir` 初始化失败时，允许降级输出到 stderr
- [x] 飞书通知失败时，最终进程退出码返回 `7`，日志保留原业务结果
- [x] 结构化日志 `message` 非字符串时转字符串
- [x] `interpreter` 允许包含参数
- [x] 业务脚本路径启动前必须存在且是文件
- [x] 缓存文件损坏时自动重建
- [x] 结构化日志路径使用 `${structured_log_root}/${date}/${job}.log`
- [x] 业务脚本每次输出追加到同一个 job 日志文件
- [x] 监视器使用执行编号和日志行数对比判断本次日志是否存在

## 3. 用户批准后实施任务

### 3.1 工程骨架

- [x] 创建 `pyproject.toml`
- [x] 创建 `README.md`
- [x] 创建 `config.example.toml`
- [x] 创建 `src/task_monitor/__init__.py`
- [x] 创建测试目录 `tests/`

### 3.2 数据结构

- [x] 定义 `GlobalConfig`
- [x] 定义 `FeishuConfig`
- [x] 定义 `JobConfig`
- [x] 定义 `RunContext`
- [x] 定义 `ProcessResult`
- [x] 定义 `StructuredLogResult`
- [x] 在 `StructuredLogResult` 中记录 `line_count`
- [x] 在 `StructuredLogResult` 中记录 `expected_number`
- [x] 在 `StructuredLogResult` 中记录 `line_count_sufficient`
- [x] 定义 `EvaluationResult`

### 3.3 配置模块

- [x] 实现 TOML 加载
- [x] 实现 Python 3.8 ~ 3.10 的 `tomli` fallback
- [x] 校验 `global` 字段
- [x] 校验 `feishu` 字段
- [x] 校验 `jobs.<job>` 字段
- [x] 处理配置错误且无法加载 Webhook 的退出路径
- [x] 编写配置模块测试

### 3.4 缓存模块

- [x] 创建缓存目录
- [x] 使用 `fcntl.flock` 保护缓存读改写
- [x] 实现同日同任务编号递增
- [x] 实现日期变化后重置
- [x] 实现缓存损坏自动重建
- [x] 记录缓存重建日志
- [x] 编写缓存模块测试

### 3.5 上下文模块

- [x] 生成 `RunContext`
- [x] 计算结构化日志路径 `${structured_log_root}/${date}/${job}.log`
- [x] 确保结构化日志路径不包含执行编号
- [x] 保留执行编号用于日志行数校验
- [x] 覆盖路径生成测试

### 3.6 执行模块

- [x] 校验业务脚本路径存在
- [x] 校验业务脚本路径是文件
- [x] 使用 `shlex.split(interpreter)` 支持解释器参数
- [x] 使用 `subprocess.Popen` 启动脚本
- [x] 使用 `wait(timeout=...)` 等待
- [x] 捕获启动失败
- [x] 捕获超时
- [x] 确保超时不终止业务进程
- [x] 编写执行模块测试

### 3.7 结构化日志模块

- [x] 读取 `${structured_log_root}/${date}/${job}.log`
- [x] 统计结构化日志物理行数
- [x] 比较 `number` 和 `line_count`
- [x] `number > line_count` 时返回结构化日志异常
- [x] `number <= line_count` 时读取最后一行
- [x] 解析最后一行 JSON object
- [x] 校验 `status`
- [x] `message` 缺失时使用空字符串
- [x] `message` 非字符串时转字符串
- [x] 处理文件不存在、空文件、JSON 非法、字段非法
- [x] 编写结构化日志测试

### 3.8 结果判定模块

- [x] 实现超时优先
- [x] 实现结构化日志优先于退出码
- [x] 将日志行数不足纳入结构化日志异常
- [x] 实现退出码兜底
- [x] 实现通知规则
- [x] 实现退出码映射
- [x] 保留原始业务状态用于通知失败日志
- [x] 编写判定矩阵测试

### 3.9 通知模块

- [x] 构造飞书 text 消息
- [x] 通知内容包含执行编号
- [x] 通知内容包含结构化日志路径
- [x] 通知内容包含结构化日志行数
- [x] 通知内容包含日志行数是否满足执行编号
- [x] 使用 `urllib.request` POST JSON
- [x] 处理 HTTP 错误
- [x] 处理网络异常
- [x] 通知失败时向编排层返回失败结果
- [x] 编写通知模块测试

### 3.10 监视器日志模块

- [x] 初始化 `${monitor_log_dir}/${date}.log`
- [x] 初始化失败时降级 stderr
- [x] 记录启动信息
- [x] 记录执行编号
- [x] 记录业务进程信息
- [x] 记录结构化日志路径
- [x] 记录结构化日志行数
- [x] 记录结构化日志解析结果
- [x] 记录最终判定和通知结果
- [x] 记录通知失败时的原始业务状态

### 3.11 编排模块

- [x] 串联配置加载
- [x] 串联日志初始化或 stderr fallback
- [x] 串联编号生成并先持久化
- [x] 串联上下文构造
- [x] 串联业务脚本执行
- [x] 未超时时读取结构化日志并校验行数
- [x] 超时时不读取后续日志作为最终结果
- [x] 串联结果判定
- [x] 串联飞书通知
- [x] 通知失败时返回退出码 `7`
- [x] 统一异常处理和退出码
- [x] 编写编排测试

### 3.12 CLI 入口

- [x] 实现 `--config`
- [x] 实现 `--job`
- [x] 对接 `orchestrator.run`
- [x] 返回进程退出码
- [x] 编写 CLI 基础测试

## 4. 验证任务

- [x] 运行 `uv sync`
- [x] 运行 `uv run pytest`
- [ ] 运行示例配置的本地成功路径
- [ ] 运行结构化日志异常路径
- [ ] 运行业务脚本失败路径
- [ ] 运行超时路径并确认业务进程未被杀死
- [ ] 运行解释器带参数路径
- [ ] 运行缓存损坏自动重建路径
- [ ] 运行日志目录不可写时 stderr fallback 路径
- [ ] 运行通知失败返回 `7` 路径
- [ ] 运行 `number > line_count` 路径
- [ ] 运行 `number <= line_count` 读取最后一行路径

## 5. 明确不做

- [ ] 不实现常驻服务
- [ ] 不实现任务调度系统
- [ ] 不实现多任务并发调度
- [ ] 不实现自动重试
- [ ] 不实现 Web UI
- [ ] 不实现数据库持久化
- [ ] 不捕获 stdout/stderr
- [ ] 不向业务脚本传递业务参数
- [ ] 不终止超时后的业务进程
- [ ] 不支持多个飞书机器人
- [ ] 不支持任务级 Webhook 覆盖
